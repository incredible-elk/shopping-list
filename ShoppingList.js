import React from 'react'
import PropTypes from 'prop-types'

ShoppingList.propTypes={store: PropTypes.string, quantity: PropTypes.number, items: PropTypes.string}


export default function ShoppingList({store, quantity, items}) {
  return (

  )
}
